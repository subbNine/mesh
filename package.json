{
  "name": "mesh",
  "private": true,
  "packageManager": "npm@10.9.2",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"turbo dev\" \"npm run dev:ws\"",
    "dev:ws": "npm run ws -w apps/api",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "turbo": "latest"
  }
}
